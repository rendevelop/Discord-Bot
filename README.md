# discord-bot

## How to Use:

You will require the Discord API installed through pip
`pip install -U discord.py`

Next, you can run the bot simply by doing:
`python bot.py`

## How to host:

Heroku

